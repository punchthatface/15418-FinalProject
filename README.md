# 15418-FinalProject
A parallel video reconstruction pipeline that reduces effective memory or transmission bandwidth by subsampling each video frame, then reconstructing missing pixels through spatial and temporal prediction. 
